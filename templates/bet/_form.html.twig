<div class="text-center">
    {{ form_start(form) }}
    {{ form_widget(form.excuse, { "attr": {"class": "form-control my-2" }})
    }}
    <button class="btn btn-sm btn-green">{{ button_label|default('Dealer 🐊') }}</button>
    {{ form_end(form) }}
</div>